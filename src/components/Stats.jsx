import React from "react";

const Stats = (props) => {
  return (
    <>
      <ul>
        <li text="good" value ={props} />
        <li text="neutral" value ={} />
        <li text="bad" value ={} />
      </ul>
    </>
  )
}

export default Stats